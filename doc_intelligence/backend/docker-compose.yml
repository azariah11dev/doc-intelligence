services:
  postgres:
    image: postgres:16
    container_name: DocIntelligenceDB_Postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - DocIntelligenceDB_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    logging:
      options:
        max-size: "10m"
        max-file: "3"
  
  qdrant:
    image: qdrant/qdrant:latest
    container_name: DocIntelligence_Qdrant
    restart: always
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333; printf \"GET /readyz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"200 OK\" <&3'"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s

  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: DocIntelligence_App
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL}
      PYTHONPATH: /app/src
      QDRANT_URL: ${QDRANT_URL}
      OLLAMA_HOST: ${OLLAMA_HOST}
    working_dir: /app
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    command: uv run uvicorn src.app:app --host 0.0.0.0 --port 8000 --reload

volumes:
  DocIntelligenceDB_pgdata:
  qdrant_data:
